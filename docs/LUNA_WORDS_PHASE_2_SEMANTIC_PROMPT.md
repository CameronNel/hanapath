# Words Phase 2 semantic marathon: frozen historical queue

This optional semantic-expansion handoff is **not active during core-app
completion**. Do not resume it from an old local branch or create new
qualification/authoring PRs.

The current scope freezes the existing Words curriculum and prioritises lesson
regression closure, full Sentence examination delivery, strict CI, and final
core acceptance.

Current work is governed only by:

[`CORE_APP_COMPLETION_ROADMAP.md`](CORE_APP_COMPLETION_ROADMAP.md)

The original handoff remains available in git history and may be reactivated
only by a new explicit owner instruction after the core release candidate.
