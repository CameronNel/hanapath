# Firebase-provisioned client API keys are public app identifiers, not authorization secrets.
# Access is enforced by Firebase Authentication / Firestore Security Rules. Keep this
# exclusion limited to the two canonical Firebase client-configuration files.
paths-ignore:
  - "firebase_config.js"
  - "mobile/android/app/google-services.json"
